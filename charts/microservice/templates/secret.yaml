{{- if .Values.global.ms.container.envs }}
apiVersion: v1
kind: Secret
metadata:
  namespace: {{ .Values.global.ms.name }}
  name: "{{ .Values.global.ms.name }}-secret"
type: Opaque
stringData:
  {{- range $key, $val := .Values.global.ms.container.envs }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
{{- end }}