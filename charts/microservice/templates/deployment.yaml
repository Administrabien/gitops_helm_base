apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ .Values.ms.name }}
  name: "{{ .Values.ms.name }}-deployment"
  labels:
    app: "{{ .Values.ms.name }}-label" #l
spec:
  replicas: {{ .Values.ms.replicas }}
  selector:
    matchLabels:
      app: "{{ .Values.ms.name }}-label" #l
  template:
    metadata:
      labels:
        app: "{{ .Values.ms.name }}-label" #l
    spec:
      containers:
        - name: "{{ .Values.ms.name }}-label" #l
          image:  {{ .Values.global.imgPath }}:{{ .Values.global.appVersion }}
          imagePullPolicy: {{ .Values.ms.container.image.pullPolicy }}
          ports:
            - name: {{ .Values.ms.container.name }}
              containerPort: {{ .Values.ms.container.port }}
              protocol: {{ .Values.ms.container.protocol }}
          env:
          {{- range $key, $val := .Values.ms.container.envs }}
          - name: "{{ $key }}"
            valueFrom:
              secretKeyRef:
                name: "{{ $.Values.ms.name }}-secret"
                key: "{{ $key }}"
          {{- end }}